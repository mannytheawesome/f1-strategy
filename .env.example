# OpenF1 live-data credentials (paid subscription).
# Copy this file to `.env` and fill in real values. `.env` is gitignored.
# data/live.py auto-loads this at import; real env vars (e.g. Railway) win.
OPENF1_USERNAME=
OPENF1_PASSWORD=
